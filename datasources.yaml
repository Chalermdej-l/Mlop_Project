
# config file version
apiVersion: 1

# list of datasources to insert/update
# available in the database
datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: 172.22.0.2:5432
    database: Monitor_DB
    user: root
    password: root
    secureJsonData:
      password: 'root'
    jsonData:
      sslmode: 'disable'